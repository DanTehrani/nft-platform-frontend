import React, { Component } from 'react';
import { Typography } from '@material-ui/core';

class WrongNetworkLabel extends Component {
  render() {
    return (
      <Typography align='center' variant='headline'>Wrong network</Typography>
    )
  }
}

export default WrongNetworkLabel;
